
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Simple profils practice",
            "file": "${workspaceFolder}/profile.html"
        }
    ]
}